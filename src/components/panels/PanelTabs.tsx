import { RoutePanel } from '@/components/ui/RoutePanel';

/**
 * Bottom-panel content: the resizable route / elevation editing panel.
 */
export function BottomPanelContent() {
    return <RoutePanel />;
}
